name: tests

on:
  push:
    branches:
      - "v*"
  pull_request:
    branches:
      - "v*"

jobs:
  test:
    uses: CursorCult/.github/.github/workflows/test-rule.yml@main
    with:
      python-version: "3.12"
